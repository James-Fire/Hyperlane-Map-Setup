namespace = hyperlane_menu

event = {
	id = hyperlane_menu.1
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		set_global_flag = generation_clusters #Default Generation type, probably going to be the most used.
		every_country = {
			limit = {
				NOT = { has_country_flag = hyperlane_menu_start }
			}
			set_country_flag = hyperlane_menu_start
			country_event = { id = hyperlane_menu.2 }
		}
	}
	
	after = {
	}
}

country_event = { ###Country Terminal
	id = hyperlane_menu.2
	title = "hyperlane_menu.2.name"
	desc = "hyperlane_menu.2.desc"
	diplomatic = yes
	custom_gui = "hyperlane_menu_window"
	is_triggered_only = yes
	
	picture_event_data = {
		portrait = ROOT.ruler
		room = ethic_spaceship_room
	}
	
	immediate = {
	}
	
	# Confirm
	option = {
		name = "hyperlane_menu.1.refresh"
		#custom_tooltip = "hyperlane_menu.1.refresh.tooltip"
		custom_gui = "hyperlane_menu_confirm"
		country_event = { id = hyperlane_menu.2 }
	}
	
	after = {
		#fire_on_action = { on_action = exploit_apply_queue }
	}
}